<div class="container">
<%= simple_form_for(@authority,
  html: { class: 'form-horizontal card' }
  ) do |f| %>

  <div class="card-header">
    <%= form_legend %>
  </div>

  <div class="card-body">
    <%= polymorphic_selector(f, 'authorizable', Wobauth.authorizable_types) %>
    <%= f.association :role %>
    <%= polymorphic_selector(f, 'authorized_for', Wobauth.authorized_for_types) %>

    <%= f.input :valid_from, as: :string, class: 'datepicker'
     %>
    <%= f.input :valid_until, as: :string, class: 'datepicker'
     %>

    <%= f.button :submit, class: 'btn btn-primary' %>
    <%= cancel_button %>
  </div>
<% end %>
</div>

<%= javascript_include_tag "wobauth/authorities" %>
