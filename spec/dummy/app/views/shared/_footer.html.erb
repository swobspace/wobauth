<%= ( current_user.nil? ) ? "not logged in" : current_user.username %>
<%= Time.now.strftime("%Y-%m-%d %H:%Mh") %> |
<%= 'dummy::' + controller.controller_name + '::' +
              controller.action_name %> 
